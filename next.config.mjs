/** @type {import('next').NextConfig} */

const nextConfig = {
  experimental: {
    appDir: true, // Usualmente esta opción es válida para habilitar el uso de la carpeta 'app'
  },
};

export default nextConfig;
